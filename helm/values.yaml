# Default values for urly.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Global settings
# -- Override the name of the chart
nameOverride: ""  
# -- Override the full name of the chart
fullnameOverride: ""  


imagePullSecrets:
# -- List of secret names to use for pulling images
  - name: "ghcr"  


# MySQL configuration
mysql:
  annotations: {}
  # -- Image pull policy for MySQL
  pullPolicy: "IfNotPresent"  
  # -- Username for MySQL
  username: ""  
  # -- Root password for MySQL
  rootPassword: ""
  # -- Password for the MySQL user
  password: ""

# Redis configuration
redis:
  pullPolicy: "IfNotPresent"  # Image pull policy for Redis
  # -- Password for Redis 
  password: ""  

# Urly web server configuration
urlyWebServer:
  image:
    repository: "ghcr.io/gr2u/url-cut-web-server"
    # -- web server  image tag
    tag: ""
    pullPolicy: "IfNotPresent"  # Image pull policy for the web server

# Urly API server configuration
urlyApiServer:
  # -- ingress host name and app_host for short url prefix hostname
  appHost: "gr2u.xyz"  
  replicas: 3  # Number of replicas for the API server
  image:
    repository: "ghcr.io/gr2u/url-cut-api-server" # API server image repository
    tag: "0.0.25-dev"  # API server image tag
    pullPolicy: "IfNotPresent"  # Image pull policy for the API server

# Select cloud provider 
# -- Cloud provider type (e.g., gcp, aws, azure), it's for different settings for ingress controller and annotations at cloud providers.
cloudProvider: "gcp"  

# Cloud provider configuration
gcp: # GCP
  annotations:
    # -- Using google ingress controller 
    kubernetes.io/ingress.class: gce 
    kubernetes.io/ingress.allow-http: "true"
    # -- Static IP name to use with ingress 
    kubernetes.io/ingress.global-static-ip-name: ingress-ip 
    # -- Cert issuer
    cert-manager.io/issuer: letsencrypt-production  



